// Phase 2 placeholder — interactive body map for muscle selection
export default function BodyMap({ highlighted = [], onSelect }) {
  return (
    <div className="w-full h-64 rounded-card bg-surface border border-hairline flex items-center justify-center text-sm text-dim">
      Interactive Body Map — Phase 2
    </div>
  );
}
